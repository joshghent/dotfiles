local options = {
  -- Only install these parsers manually via :TSInstall
  ensure_installed = {},

  highlight = {
    enable = true,
  },

  indent = { enable = true },
  sync_install = false,
  auto_install = false,
}

return options
