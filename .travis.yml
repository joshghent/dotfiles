sudo: required

language: bash

services:
  - docker

before_install:
  - docker pull koalaman/shellcheck

script:
  - docker run --rm --name shellcheck -v $(CURDIR):/usr/src:ro --workdir /usr/src koalaman/shellcheck ./test.sh

notifications:
    webhooks: https://www.travisbuddy.com/
    on_success: never
